# Changelog

## 2026-08-18

- 修复旧数据库升级时在新增复习字段之前创建索引、可能导致启动失败的问题。
- 区分普通证据编辑和明确复习动作，避免编辑到期卡片时误推进复习计划。
- 新增复习调度回归测试，并记录旧 schema 覆盖升级的人工验证项。
- 修复资料删除成功后 PDF 删除归档未清理、可能持续占用本地空间的问题。
- 收紧 PDF 与网页证据卡的类型、回跳页和选区页约束，并增加健康检查与备份回归测试。
- 将应用沙箱内的备份快照和 PDF 导入改为临时文件写完后再移动，避免中断留下可被误读的半文件。
- 删除失败时保护已有 PDF，不覆盖未知文件；保留数据库主错误并报告回滚失败原因。
- 收紧备份、Markdown 和诊断导出的写入/关闭错误传播；删除归档恢复失败时显示需要检查文件状态的提示。
- 启动时清理阅迹管理的导入、PDF 修复和备份临时文件，避免中断写入残留占用或被误读。
- PDF 丢失修复改为校验临时副本后原子替换，并一次性更新来源指纹、页数和阅读位置。
- 备份恢复界面区分事务已回滚、回滚未确认、数据已写入但界面未完整重载等失败状态。
- 新增临时文件识别和事务失败状态回归测试，本轮 API 24 数据完整性测试总数达到 30 个。
- 批量复习和批量追加标签改为使用事务实际写入的完整卡片回填界面，列表重载失败时仍保持复习时间、次数和标签一致。
- 单卡证据编辑、网页证据编辑、复习状态切换和延期复习统一返回完整持久化卡片，列表重载失败时直接回填数据库实际状态。
- 新增证据卡变更服务，集中复用单卡与批量更新规则，补充复习计划保持、网页字段规范化和批量标签规范化测试。
- 本轮 API 24 数据完整性单元测试总数更新为 30 个，新增单卡证据写入一致性回归覆盖。
- 复习中心的标签筛选同时覆盖资料标签和证据卡标签，关键词搜索补充资料网址与资料标签。
- PDF 开始新搜索时立即清除旧的结果计数，并在未打开 PDF 时显示明确提示。
- 删除证据卡后同步清理批量选择并关闭可能失效的对比视图。

## Unreleased - 2026-08-18

- 为资料、证据卡、收藏和复习查询增加数据库索引与资料删除级联清理
- 强化资料更新/删除的结果校验，避免静默更新或删除失败
- 校验证据选区结构、页码和坐标，明确展示来源丢失与选区不可恢复状态
- 增加标题、网址、摘录、笔记、标签、搜索和证据原文的长度边界
- 改进网址校验、标签建议、非 PDF 拖放提示和 Markdown 导出文件名/内容
- 增加跨资料主题集合、证据卡只读对比和批量复习/标签操作
- 增加 JSON 结构化备份与恢复，恢复前快照、删除前快照和 PDF 删除归档
- 增加数据库 schema 版本标记、升级迁移、索引和孤儿数据清理
- 增加数据健康检查、缺失 PDF/孤儿关系提示和脱敏诊断导出
- 增加备份路径标准化，避免跨设备恢复后引用旧设备沙箱路径
- 补充数据完整性、备份恢复和诊断脱敏单元测试

## 1.0.0 - 2026-08-14

- 建立 HarmonyOS PC 本地资料库
- 支持文件选择和拖放导入 PDF
- 支持 PDF 缩放、适应宽度、页码跳转、全文搜索和缩略图
- 支持高亮、下划线和多颜色证据卡
- 支持持久化选区与来源回跳
- 支持资料搜索、标签、收藏和最近阅读
- 支持网页资料卡与系统浏览器打开来源
- 支持待复习、已掌握和标签筛选
- 支持带来源信息的 Markdown 导出
- 支持暗色模式、专注模式、快捷键和窄窗口侧栏
- 加入阅迹品牌图标、隐私政策和发布材料
